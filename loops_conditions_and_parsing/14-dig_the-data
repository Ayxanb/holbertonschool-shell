#!/bin/bash

# Script to group visitors by IP and HTTP code
# Output format: OCCURRENCE_NUMBER IP HTTP_CODE

awk '{ key = $1" "$9; count[key]++ } END { 
        for (k in count) print count[k], k 
    }' apache-access.log | sort -nr
