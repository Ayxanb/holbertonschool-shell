#!/usr/bin/env bash
# Parse the Apache log, group by IP and HTTP code, count occurrences, and sort
awk '{count[$1" "$9]++} END {for (key in count) print count[key], key}' apache-access.log | sort -nr
